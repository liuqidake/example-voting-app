name: Trigger auto deployment for container-app-ok5oxpeq3q7au

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'vote/**'
    - '.github/workflows/container-app-ok5oxpeq3q7au-AutoDeployTrigger-543a8446-db8d-497c-87fa-d5ceec834946.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPOK5OXPEQ3Q7AU_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPOK5OXPEQ3Q7AU_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPOK5OXPEQ3Q7AU_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/vote 
          registryUrl: containerregistryok5oxpeq3q7au.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPOK5OXPEQ3Q7AU_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPOK5OXPEQ3Q7AU_REGISTRY_PASSWORD }}
          containerAppName: container-app-ok5oxpeq3q7au
          resourceGroup: examplevotingappa2b9
          imageToBuild: containerregistryok5oxpeq3q7au.azurecr.io/container-app-ok5oxpeq3q7au:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


